<?xml version="1.0" encoding="UTF-8"?>
<!--
  OWASP Dependency-Check — False Positive Suppressions
  =====================================================
  Add entries here to suppress known false positives or accepted risks.

  Format reference:
  https://jeremylong.github.io/DependencyCheck/general/suppression.html

  Example entry (uncomment and adapt when needed):

  <suppress>
    <notes>
      CVE-XXXX-YYYY: False positive — this CVE affects the C library, not the
      npm package of the same name. Verified on YYYY-MM-DD by <name>.
    </notes>
    <packageUrl regex="true">^pkg:npm/package-name@.*$</packageUrl>
    <cve>CVE-XXXX-YYYY</cve>
  </suppress>
-->
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">

  <!--
    Add project-specific suppressions below as false positives are identified.
    Each suppression must include:
      1. <notes> explaining why it is suppressed and who approved it
      2. The narrowest possible scope (prefer <cve> over <cvssBelow>)
      3. A review date — suppressions should be re-evaluated at each release
  -->

  <!--
    GHSA-qwww-vcr4-c8h2 / react-router RSC Mode CSRF Bypass
    =========================================================
    Accepted risk — NOT EXPLOITABLE in this project.

    Advisory: https://github.com/advisories/GHSA-qwww-vcr4-c8h2
    Affected:  react-router 7.12.0 – 8.2.0
    Fix:       react-router 8.3.0+ (major version upgrade)

    Reason for suppression:
      This CSRF bypass only affects applications using the UNSTABLE React
      Server Components (RSC) APIs (e.g., unstable_RSCContext, server actions
      in RSC mode). cloudcert-pro does not use any RSC APIs — it is a
      standard SPA using react-router-dom v7 with client-side routing only.
      Confirmed by grepping the src/ tree for "use server", "use client",
      "unstable_rsc", and related RSC primitives — none found.

    Reviewed: 2026-07-31
    Approved by: Rajesh
    Re-evaluate when upgrading react-router-dom to v8.x.
  -->

</suppressions>
